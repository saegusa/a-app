# 実装ログ

## 2026-02-06
- Angular 最新系（v21 想定）でプロジェクト構成ファイルを追加。
- 将棋ロジックを `src/lib/shogi.ts` に集約。
- 盤面 UI と CPU 対戦ループを `AppComponent` に実装。
- README とオンボーディングを MVP 方針に合わせて更新。
- 依存取得（npm レジストリ）に 403 が出るため、ビルド/監査実行までは未完。

## 次アクション
1. レジストリアクセス可能環境で `npm install`。
2. `npm run start` で操作確認。
3. ルールの完全性（王手回避、二歩以外の打ち制約）を拡張。

- 脆弱性ゼロ方針に合わせ、`npm run audit` と `overrides` を追加。
